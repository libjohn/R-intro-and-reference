# Correlation Plot[^1]

## Load Data
```{r loaddata}
file <- "https://github.com/smach/NICAR15data/raw/master/testscores.csv"
testdata <- read.csv(file, stringsAsFactors = FALSE)
```

## Make the graph
```{r correlate}
## Correlation plot
require(ggvis)
ggvis(testdata, ~ pctpoor, ~ score) %>%
  layer_points(size := input_slider(10, 310, label = "Point size"), opacity := input_slider(0, 1, label = "Point opacity")) %>%
  layer_model_predictions(model = "lm", stroke := "red", fill := "red")
```

### Note:
An interactive slide-bar show up in the Viewer pane in RStudio.  Sadly, the slider does not get rendered into the HTML view.  Still, pretty cool.


####Credit: 
[^1]: [5 Visualizations in 5 Minutes](http://www.computerworld.com/article/2893271/business-intelligence/5-data-visualizations-in-5-minutes-each-in-5-lines-or-less-of-r.html). ComputerWorld.com by Sharon Machlis
